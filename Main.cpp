#include <iostream>

int main() {
	std::cout << "Hello World!" << std::endl;

	char* test;

	// This is going to be identical to my first implementation of this project, except this one is going to have a slightly nicer CLI,
	// which I am going to actually make a class for rather than it being hard coded

	std::cin >> test;
	
	return 0;
}
